<table style="border-collapse:collapse;width:100%;margin:0 0 20px 0;table-layout:fixed;">
  {% for product in products %}
    {% if loop.index0 % 2 == 0 %}
    <tr>
    {% endif %}
      <td style="padding:5px 10px;">
          <a  href="{{ storeURL }}index.php?route=product/product&product_id={{ product.wishlist_product_id }}" style="display:block;padding:0;margin:0 0 5px 0;font-size:0;text-align:left;">
          <img src="{{ images[product.wishlist_product_id] }}" alt="{{ product.product_name }}" style="font-size:0;padding:0;margin:0;border:none;text-align:left;"/> </a>
          <a style="text-decoration:none;color:#000;display:block;padding:0;margin:0 0 5px 0;font-size:14px;" href="{{ storeURL }}index.php?route=product/product&product_id={{ product.wishlist_product_id }}" target="_blank"> {{ product.product_name }} </a>
          <span style="font-size:12px;color:#000;display:inline-block;margin:0 0 5px 0;"> {{ currencyLeft ~ product.regular_price ~ currencyRight }} </span>
      </td>
    {% if loop.index0 % 2 == 0 %}
    </tr>
    {% endif %}
  {% endfor %}
</table>
